% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_pubmed.R
\name{fetch_pubmed}
\alias{fetch_pubmed}
\title{Retrieve PubMed references and optionally write to a BibTeX file}
\usage{
fetch_pubmed(
  ...,
  sep = "|",
  results = 20,
  field = NULL,
  output = "references.bib",
  write = FALSE,
  append = TRUE
)
}
\arguments{
\item{...}{One or more PubMed search terms or PMIDs. Multiple values are
combined using \code{sep}.}

\item{sep}{Character used to combine multiple search terms. Defaults to \code{"|"},
which performs an OR search.}

\item{results}{Maximum number of records to retrieve. Defaults to \code{20}.}

\item{field}{Optional PubMed search field (e.g., \code{"PMID"}, \code{"author"},
\code{"title"}). Passed to \code{RefManageR::ReadPubMed()}.}

\item{output}{Path to the BibTeX file when \code{write = TRUE}.}

\item{write}{Logical indicating whether to write the references to a BibTeX
file.}

\item{append}{Logical indicating whether to append to an existing BibTeX file.}
}
\value{
A \code{BibEntry} object.
}
\description{
Searches PubMed using one or more query terms or PMIDs and returns the
resulting references as a \code{BibEntry} object. References can optionally be
written to a BibTeX (\code{.bib}) file.
}
